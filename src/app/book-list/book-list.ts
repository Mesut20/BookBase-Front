import { Component } from '@angular/core';

@Component({
  selector: 'app-book-list',
  standalone: true,
  template: '<p>Book List (under construction)</p>',
  styles: ['p { color: blue; }']
})
export class BookListComponent {} // Se till att 'export' finns